<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlayGround" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/brightblue</item>
        <!--        <item name="colorPrimaryDark">@color/DarkBlue</item>-->
        <item name="android:textColor">@color/brightblue</item>
        <item name="colorOnPrimary">@color/skyblue</item>
        <item name="colorPrimaryDark">@color/azure</item>
    </style>

    <style name="Theme.PlayGround" parent="Base.Theme.PlayGround" />

    <style name="ThemeOverlay.PlayGround.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
    <style name="CustomDateTimePickerDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog">
        <!-- Customize the colors as per your preference -->
        <item name="colorAccent">@color/brightblue</item>
        <item name="android:textColorPrimaryInverse">@color/white_variant</item>
        <item name="android:textColorPrimary">@color/gray50</item>
        <item name="android:textColorSecondary">@color/gray50</item>
    </style>

    <style name="customTemp" parent="Theme.MaterialComponents">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="BottomNavigationView" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorSecondaryContainer">@color/white</item>

    </style>
</resources>